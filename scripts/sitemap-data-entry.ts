// Entry point bundled by scripts/generate-sitemap.mjs to expose the catalogue
// data (TypeScript + path aliases) to a plain Node script.
export { ALL_PRODUCTS } from "@/data/index";
export { COMPANIES } from "@/data/companies/index";
export { NEWS_ITEMS } from "@/data/news";
